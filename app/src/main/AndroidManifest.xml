<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnyDoubt"
        tools:targetApi="31">

        <!-- Add this meta-data tag with your AdMob App ID -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3734613584513892~2940617399"/>

        <property
            android:name="android.adservices.AD_SERVICES_CONFIG"
            android:resource="@xml/gma_ad_services_config"
            tools:replace="android:resource" />

        <!-- CRITICAL FIX: Add this property with tools:replace to resolve the conflict -->
        <property
            android:name="android.adservices.AD_SERVICES_CONFIG"
            android:resource="@xml/gma_ad_services_config"
            tools:replace="android:resource" />

        <!-- Splash Activity (LAUNCHER) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AnyDoubt">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.AnyDoubt" />

        <!-- Internet Check Activity -->
        <activity
            android:name=".InternetCheckActivity"
            android:exported="false"
            android:theme="@style/Theme.AnyDoubt" />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.AnyDoubt" />

        <!-- Answer Activity -->
        <activity
            android:name=".AnswerActivity"
            android:exported="false"
            android:theme="@style/Theme.AnyDoubt" />

        <!-- Analyzing Loading Activity -->
        <activity
            android:name=".AnalyzingLoadingActivity"
            android:exported="false"
            android:theme="@style/Theme.AnyDoubt" />

        <!-- Gallery Activity -->
        <activity
            android:name=".GalleryActivity"
            android:exported="false"
            android:theme="@style/Theme.AnyDoubt" />

        <!-- Note Detail Activity -->
        <activity
            android:name=".NoteDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.AnyDoubt" />

        <!-- Update Activity -->
        <activity
            android:name=".UpdateActivity"
            android:exported="false"
            android:theme="@style/Theme.AnyDoubt" />

    </application>

</manifest>